---
import { it } from "../../i18n/it";
import Landing from "../../components/Landing.astro";
import rootPkg from "../../../../package.json";

const RELEASE_TAG = `v${rootPkg.version}`;
const releaseUrl = `https://github.com/cedoor/nesso/releases/tag/${RELEASE_TAG}`;
---

<Landing t={it} lang="it" releaseTag={RELEASE_TAG} releaseUrl={releaseUrl} />
