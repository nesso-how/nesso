---
description: Run CI-parity checks locally (format, lint, types, builds, license headers, Playwright e2e) before opening or updating a PR. Use before pushing to catch a red CI early. Run conditional mutation tests when the diff touches Stryker areas; run native e2e (Docker) opt-in when the diff touches desktop/fs sync.
alwaysApply: false
---

Canonical procedure lives in the preflight skill at [`.claude/skills/preflight/SKILL.md`](../../.claude/skills/preflight/SKILL.md). Follow it step by step. Edit that file, not this wrapper.

@.claude/skills/preflight/SKILL.md
